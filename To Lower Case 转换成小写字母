char * toLowerCase(char * str)
{
    int i = 0;
    int len = 0;
    char *p = str;
    len = strlen(str);

    for(i = 0; i<len;i++)
    {
        if((*(p+i) >= 65) && (*(p+i) <= 90))
        {
            *(p+i) = *(p+i) + 32;
        }

    }

    return str;

}
